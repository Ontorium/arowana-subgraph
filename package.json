{
    "name": "arowana-subgraph",
    "version": "0.0.1",
    "license": "MIT",
    "scripts": {
        "codegen": "graph codegen",
        "build": "graph build",
        "deploy-dev": "graph deploy arowana-dev  --ipfs https://ipfs.network.thegraph.com -l",
        "deploy-stage": "graph deploy arowana-stage  --ipfs https://ipfs.network.thegraph.com -l",
        "deploy-prod": "graph deploy arowana-prod  --ipfs https://ipfs.network.thegraph.com -l"
    },
    "dependencies": {
        "@graphprotocol/graph-cli": "0.98.1",
        "@graphprotocol/graph-ts": "0.35.1"
    }
}
